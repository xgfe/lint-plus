//no-mixed-spaces-and-tabs
// Bad 
function add(x, y) {
// --->..return x + y;
    return x + y;
}

function main() {
// --->var x = 5,
// --->....y = 7;
    var x = 5,
        y = 7;
}

// Good 
function add(x, y) {
// --->return x + y;
    return x + y;
}